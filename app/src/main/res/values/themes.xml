<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Overwatch2App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Overwatch2App" parent="Base.Theme.Overwatch2App" />

    <!-- Define un nuevo estilo para el SearchView -->
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Desactiva la línea inferior -->
        <item name="android:queryBackground">@android:color/transparent</item>

    </style>

</resources>